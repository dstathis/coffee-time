# Shared credentials for all participants (HTTP Basic Auth)
APP_USERNAME=coffee
APP_PASSWORD=changeme

# Password required to access the admin page
ADMIN_PASSWORD=admin-secret

# Flask session secret key (auto-generated if omitted)
# SECRET_KEY=some-random-string

# SQLite database path (default: coffee.db in the project root)
# DATABASE_URL=coffee.db
